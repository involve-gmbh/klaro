version: '3.5'
services:
  serve:
    build: docker/npm
    working_dir: /app
    command: run dev
    ports:
      - "8000:8000"
    volumes:
      - .:/app
  npm:
    build: docker/npm
    working_dir: /app
    command: i
    volumes:
      - .:/app
